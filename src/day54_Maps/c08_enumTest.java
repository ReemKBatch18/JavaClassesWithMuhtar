package day54_Maps;

public class c08_enumTest {

    public static void main(String[] args) {

        // because we only created Red, Green, Blue, we can only use one of them here
        Color myFavColor = Color.Blue;
      //  Color myFavColor2 = Color.Yellow;
        // error because we didn't assign yellow to Color before in enum
        System.out.println(myFavColor);


        c09_Browsers browserName = c09_Browsers.Chrome;
        // when I make data type as browsers I can only assign from the ones created only











    }
}
